<% if flash.any? %>
  <% flash.each do |message_type, message| %>
    <div class="container">
      <div class="alert alert-<%= message_type %> mt-3">
        <%= message %>
      </div>
    </div>
  <% end %>
<% end %>